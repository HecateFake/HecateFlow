# 实施计划:{{TITLE}}

> 临时计划文件(plan→build 工作流)。进 build 第一步写入;会话恢复先读本文件确认进度;**全部完成、验证通过并经用户确认处理 Git 后删除**,不长期保留(区别于 PROJECT.md)。

## 目标 target

{{TARGET_ID}}（协作模型:{{COLLAB_MODEL}};复杂度:{{ORCHESTRATION_LEVEL}}）

## 已确认的用户决策

1. {{DECISION_1}}

## 事实来源与假设

- 已证实事实:{{代码/日志/用户复述/SDK 实现/硬件现象中已确认的事实}}
- 待二次确认:{{用户提出"不可能出现"或 SDK/provider 行为假设时,列出需确认的问题}}
- 未证实假设:{{仍未证实,不得直接作为修复依据}}

## 需求重述

{{REQUIREMENT}}

## 核心设计决策

- {{DESIGN_DECISION}}

## 分层与文件拆分

- 硬件底层:{{HW_LOW_FILES}}(pin/bus/register/vendor adapter/init/config)
- 硬件顶层:{{HW_TOP_FILES}}(device facade / 单位状态语义 / owner API)
- 软件实现:{{SOFTWARE_FILES}}(算法 / 控制 / 应用策略)
- 公共库 / 公共头候选:{{REUSABLE_LIBRARY_CANDIDATES}}(<650 行但高频复用时也可提前封装)
- 行数门:{{FILE_SPLIT_GATE}}(>650 行需评估;>1000 行默认拆分,若为特殊长文件须记录用户确认例外)

## 通信 / 共享快照 / 持久化

- 总线/协议契约:{{BUS_CONTRACT}}(唯一 master / request-response / timeout / valid frame / 失链降级)
- RX 预算:{{RX_BUDGET_PLAN}}(ISR 只入缓冲/置标志;前台 byte/frame budget)
- 快照新鲜度:{{SNAPSHOT_FRESHNESS}}(`magic` / `seq` / freshness gate / 失链置零或软停)
- 外部命令导数:{{RX_SEQ_FEEDFORWARD}}(仅新 rxSeq 边沿计算;首帧/超时锚定)
- 参数持久化:{{PARAM_PERSISTENCE}}(`magic/version/payloadBytes/CRC`;load defaults;version 迁移/写回门;CRC/magic 错不覆盖;flash 写入不进热路径)

## 协作与 worker 边界

- 主 agent 责任:先自主求证,保留最终上下文、代码主权、方案裁决和 Git 写权限。
- 只读调研/复审:{{READ_ONLY_AND_REVIEW_PLAN}}(L1-L3 主动派发只读子代理,不需额外用户确认)。
- 复审迭代闭环:{{REVIEW_ITERATION_PLAN}}(复审 FAIL/CRITICAL/HIGH/MEDIUM 覆盖缺口先修复再复审,直到当前 change set PASS 或只剩 A3 用户确认/物理验证/平台限制)。
- worker 启用门:用户已明确要求实现/修改/落地/应用补丁,且方案确定、文件范围互斥、worker 边界明确;只读规划/复审不能因为研究充分就使用写入 worker。
- worker 文件范围:{{WORKER_FILE_SCOPE}}(必须互斥且可独立验证)。
- worker 禁止事项:{{WORKER_FORBIDDEN_ACTIONS}};固定禁止 stage / commit / push。
- worker 接口预期:{{WORKER_INTERFACE_EXPECTATION}}。
- worker 验证方式:{{WORKER_VALIDATION_METHOD}}。
- 最小提问点:{{MINIMAL_QUESTIONS}}(外部不可证事实/安全边界/硬件/物理风险/写入 worker 越权风险/Git/远端历史变更)。
- Git 确认门:完成实现和验证后只输出摘要(summary)、验证结果(tests)、建议提交说明(suggested commit message)与待暂存文件(files-to-stage);仅在用户对当前 change set 明确确认后再进入 Git 写流程。

## 实施步骤(进度勾选）

- [ ] 阶段 1:{{STAGE_1}}
- [ ] 阶段 2:{{STAGE_2}}
- [ ] 阶段 3:{{STAGE_3}}
- [ ] 分层/分文件登记:新增硬件底层、硬件顶层、软件实现或公共库文件后,同步构建系统 + LSP `-I`

每个阶段完成后:跑 hf-auto-workflow 审查 → 同步 PROJECT.md → 勾选本框。

## 切点核对(来自模块设计卡）

- [ ] 源文件登记(hf-build-sync)
- [ ] 分层/行数门(>650 行评估;>1000 行拆分或用户确认特殊长文件例外)
- [ ] 通信/共享快照门(master/request-response/RX budget/`magic`/`seq`/freshness/失链降级)
- [ ] 参数持久化门(`magic/version/payloadBytes/CRC`/fail-closed/flash 写入时机)
- [ ] 复审迭代闭环(FAIL 后修复并重新复审到 PASS 或列明 A3 剩余项)
- [ ] 宏/构建变体
- [ ] ISR 路由
- [ ] 外设门控
- [ ] volatile
- [ ] 文档同步

## 验收标准

- {{ACCEPTANCE}}

## 完成动作

- [ ] 全部阶段勾选完毕
- [ ] PROJECT.md 已同步
- [ ] 已输出 Git 建议(summary/tests/suggested commit message/files-to-stage);等待用户确认当前 change set 后再 stage / commit / push
- [ ] **删除本计划文件**
