# {{TARGET_ID}} — {{TARGET_ROLE}}

> 本 target 的单一事实真相源。无上下文的 agent 应能只读本文件在该 target 内独立工作。
> 必须随代码同步更新(见 hf-doc-discipline)。

## 状态卡

| 项 | 值 |
|----|----|
| 分支 | {{BRANCH}} |
| 构建变体 / 模式 | {{BUILD_MODE}} |
| 独占外设所有权 | {{PERIPHERAL_OWNERSHIP}} |
| 活跃任务 | {{ACTIVE_TASK}} |
| 已知坑 | {{KNOWN_PITFALLS}} |

## 0. 协作边界

- 默认自主探索:查看、审查、调研、计划、解释时先读码/搜索/查 manifest/看 diff/跑只读命令,不把可发现事实问给用户;只有用户已明确要求实现/修改/落地/应用补丁才进入可写模式。
- 当前活跃 agent / 任务记录:{{ACTIVE_AGENT_TASKS}}。
- 写入 worker 文件范围:{{WORKER_FILE_SCOPE}};只有用户已明确要求实现/修改/落地/应用补丁且方案确定、文件范围互斥、worker 边界明确时才可启用;只读规划/复审不能因为研究充分就使用写入 worker。
- 高风险任务(ISR、跨核、协议、安全、行为保持重构、硬件 owner/极性):按 L2/L3 主动派发多路只读调研 + 复审子代理 + 主 agent 亲验。
- 最小提问:只问外部不可证事实、安全边界、硬件/物理风险、写入 worker 越权风险和 Git/远端历史变更。
- Git 确认门:子代理和 worker 永不 stage / commit / push;主 agent 完成验证后只先报告摘要(summary)、验证结果(tests)、建议提交说明(suggested commit message)和待暂存文件(files-to-stage);仅在用户确认当前 change set 后才 stage / commit / push。

## 1. 身份与定位

- MCU / 架构:{{MCU}} / {{ARCH}}
- 工具链:{{TOOLCHAIN}}
- 职责:{{ROLE_DETAIL}}
- 在系统中的分工:{{SYSTEM_ROLE}}

## 2. 模块清单(源码目录）

目录结构简述:{{DIR_LAYOUT}}

| 文件 | 用途 |
|------|------|
| {{FILE}} | {{PURPOSE}} |

### 分层 / 分文件记录

复杂功能按硬件底层、硬件顶层、软件实现分开封装。自写业务文件超过 650 行需记录分文件评估;超过 1000 行默认拆分或封装管理,除非 vendor/generated/table 或用户明确确认特殊长文件例外并记录理由。小于 650 行但高频复用的模块可提前提为公共库或公共头。

| 模块 | 硬件底层 | 硬件顶层 | 软件实现 | 行数/例外说明 |
|------|----------|----------|----------|---------------|
| {{MODULE}} | {{HW_LOW_FILES}} | {{HW_TOP_FILES}} | {{SOFTWARE_FILES}} | {{FILE_SPLIT_NOTE}} |

### 代码级驱动 owner

同一物理硬件驱动实例尽量只归一个 owner 模块,用对象式实例负责 init/config/static state/update;其它模块通过 owner API、接口结构或 init 期绑定访问,避免竞态和管理混乱。

| 硬件 / 驱动实例 | owner 模块 / 对象 | 其它模块访问方式 |
|----------------|-----------------|------------------|
| {{DRIVER_DEVICE}} | {{DRIVER_OWNER}} | {{DRIVER_ACCESS}} |

### 通信 / 共享快照

半双工/共享总线优先唯一 master + request-response + timeout + valid frame;通信 ISR 只收字节/入缓冲/置标志,前台有限额解析;跨核/跨 ISR 命令用 `magic`/`seq`/freshness gate,失链/超时不消费旧命令。

| 链路 / 快照 | master / owner | 新鲜度字段 | 失链/超时动作 |
|-------------|----------------|------------|---------------|
| {{COMM_LINK}} | {{COMM_MASTER}} | {{COMM_FRESHNESS}} | {{COMM_TIMEOUT_ACTION}} |

### 参数持久化

运行时参数 blob 采用 `magic/version/payloadBytes/CRC`;上电先加载默认值,仅可迁移 version mismatch 在写回门允许时覆盖 flash;CRC/magic/payloadBytes 错误不自动覆盖,flash erase/write 不进入 ISR/控制热路径。

| 参数域 | magic | version | 写回门 / 迁移说明 |
|--------|-------|---------|-------------------|
| {{PARAM_DOMAIN}} | {{PARAM_MAGIC}} | {{PARAM_VERSION}} | {{PARAM_WRITEBACK_GATE}} |

## 3. 高危文件 / 引脚总览

跨 target 同名不同义文件(编辑前必须公告 target):

| 文件 | 本 target 含义 |
|------|---------------|
| {{HAZARD_FILE}} | {{HAZARD_MEANING}} |

引脚 / 硬件接口:{{PINOUT_REF}}

## 4. 边界与约束

- **{{TOPIC}}**:{{CONSTRAINT}}

## 5. ISR / 时序

| ISR / 周期 | 职责 |
|-----------|------|
| {{ISR}} | {{ISR_DUTY}} |

## 6. 关键参数

| 参数 | 值 | 说明 |
|------|----|----|
| {{PARAM}} | {{VALUE}} | {{PARAM_NOTE}} |

## 验证清单

- [ ] {{VERIFY_ITEM_1}}
- [ ] {{VERIFY_ITEM_2}}
- [ ] {{VERIFY_ITEM_3}}
